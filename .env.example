# Backend API URL for local development with SAM
# When running 'sam local start-api', the API is available at http://localhost:3000
# For production, update this to your deployed API Gateway URL
VITE_API_URL=http://localhost:3000
